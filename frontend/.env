# Local development — points to Django on port 8000
VITE_API_URL=http://localhost:8000/api/v1

# Production — replace with your Render URL after deploying Django
# VITE_API_URL=https://brewhaven-api.onrender.com/api/v1
